apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ${{ parameters.name }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  target:
    name: ${{ parameters.name }} # name of resulting Kubernetes Secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: ${{ parameters.name }}
