apiVersion: v1
kind: Service
metadata:
  name: ${{ parameters.name }}
  labels:
    backstage.io/kubernetes-id: ${{ parameters.name }}
spec:
  selector:
    app: ${{ parameters.name }}
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
