apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ${{ values.name }}
  labels:
    backstage.io/kubernetes-id: ${{ values.name }}
spec:
  parentRefs:
    - name: gateway # Must match the Gateway name
      namespace: istio-system # Must match the Gateway namespace
  hostnames:
    - ${{ values.name }}.bergtobias.com # Must be consistent with your Gateway’s hostname(s) if you set any
  rules:
    - backendRefs:
        - name: ${{ values.name }} # The Service receiving traffic
          port: 80
