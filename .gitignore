**/node-token


# Local .terraform directories
.terraform/

# Terraform state files
*.tfstate
*.tfstate.backup

# Crash logs
crash.log

# Override files (usually local, ignore these)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Sensitive variable files
*.tfvars
*.tfvars.json

# CLI configuration files
.terraformrc
terraform.rc

/terraform/secrets

# Ignore files (not folders) directly under /secrets
*/secrets/*

# But do NOT ignore directories under /secrets (unignore all directories)
*/secrets/*/

# Explicitly allow all files/folders inside /secrets/examples/
!*/secrets/examples/
!*/secrets/examples/**

# (Optional) if you want to allow hidden files inside /secrets/examples
!*/secrets/examples/.*

# Ignore anything with "secret" in the name
**/*secret*
**/secret*/**

# But allow example secrets
!**/secret.example.yaml
!**/secret.*.example.yaml